function ClassRenderer(className) {
	if(className === undefined)
		return null;

	this.settings = $.extend({
		foo: undefined
	}, options );
	
	this.clear();
	this.initializeClickHandlers();
}

History.prototype.history = [];
History.prototype.pos = -1;
History.prototype.settings = {};
}